# TODO

**残っている項目: TODO-006。** これまでに 5 件を決着させた。
新しく足すときは「完了済み」の上に節を作る。**番号は `TODO-007` から。**

---

## TODO-006. ヘッダのスコアなどの文字を大きくする

|      | main | 担当 |
|------|------|------|
| 見込み | Opus 5.5 / effort high | main（実装）+ verifier（Sonnet 5 / medium） |

- [ ] `.hud` の `font-size` を今の倍（PC で `1.9rem` ほど）にし、`--hud-h` を合わせて高くする
- [ ] スマホの幅では 1 行に収まる大きさで止める（今の 1.4 倍ほど）
- [ ] PC とスマホの幅で、はみ出さず 1 行に収まることを Playwright で実測する

ヘッダ（SCORE / HI / WAVE / LIVES）の文字が小さすぎる。今は
`clamp(0.65rem, 2.8vw, 0.95rem)`（`src/pages/index.astro`）。
倍にすると幅 360px のスマホでは約 34 文字が 1 行に収まらない。2 行にする、
ラベルを短くする案もあったが、1 行のまま収まる大きさで止めると決めた。
ゲーム画面の大きさはヘッダの高さから逆算しているので、その分だけ小さくなる。
CSS の値だけの変更なので reviewer は入れない。

---

## 完了済み

決着した項目は `archives/todo/` にある（1 項目 1 ファイル）。
一覧は [archives/index.md](archives/index.md)（新しい順）。やらないと決めたものは
ファイル名に（対応しない）が付いていて、理由も書いてある。蒸し返す前に読むこと。
