# TODO-002 verifier への依頼

仕様は `TODO.md` の TODO-002 の節。コードは直さない。

## 1 回目: 直す前の再現（今の HEAD のまま）

計測スクリプトは `archives/agents/TODO-002/verify-fire.mjs` に 1 本だけ作る
（`archives/agents/TODO-001/verify.mjs` から必要な部分を写してよい。TODO-001 のファイルは変えない）。
2 回目もこのスクリプトをそのまま使うので、判定は数値で出るようにする。

条件: `npm run build` のあと `npm run preview`、デスクトップ 1280x800、Enter で開始した直後
（敵の弾に当たる前に測り終える。被弾したらその回は捨てて数え直す）。

弾の数え方: canvas の `getImageData` で、自機の真上の細い帯（自機の中心 x ± 4px、
自機の上端から上 200px）の明るい画素を数え、弾がある・無いを判定する。
**帯の画素だけで割合を出す**（TODO-001 では canvas 全体で割って検出できなかった）。
自機の弾の色を game.ts で確かめ、敵の弾と取り違えないようにする。

測るもの:

1. **すぐ離す**: `keyboard.down('Space')` と `keyboard.up('Space')` を間を空けずに送る
   （`keyboard.press` でもよい。使った方を書く）。そのあと 50ms 待って弾があるか。
   1 回ごとに 0.6 秒空けて 20 回。弾が出た回数 / 20
2. **押しっぱなし**: 0.5 秒 down してから up。出た弾の数（0.35 秒間隔なので 2 発前後のはず）
3. **撃てない間の押下**: すぐ離す押下を 1 回、その 0.15 秒後にもう 1 回。2 回目の押下から
   0.4 秒以内に 2 発目が出たか。10 回

比べるため、1 は `down` と `up` の間を 50ms 空けた場合も 20 回測る。

## 報告

`archives/agents/TODO-002/verifier-report.md` に、測った値を表で。
返事は「終わったか・報告ファイルのパス・判断が要る点」の 5 行以内。
境界線上の判断は報告だけ。原因の切り分けはしない。preview サーバは終わったら止める。

## 2 回目: 直した後の確認（作業ツリーの `src/game/game.ts`）

reviewer の報告 `reviewer-report.md` の指摘 4 を受けて、測り方を直してから測る。

- 弾の判定: 自機をシールドの間に動かしてから測る（シールドの位置は game.ts で確かめる）。
  そのうえで、押してから 0.5 秒の間、毎フレームかそれに近い間隔で帯を見て「一度でも弾が
  写ったか」で判定する（1 回だけ見るのをやめる）。撃てない間の押下を測るときは、弾の数を
  数えられるようにする（白の塊の数、または押してからの時刻ごとの記録）
- 直した測り方で、**直す前の値も 1a・1b だけ測り直す**（`git stash` は使わない。
  `git show HEAD:src/game/game.ts` を scratchpad に写してビルドし直すなど、作業ツリーを
  変えない方法で。やり方を報告に書く。難しければ直す前の測り直しは省いてよく、その旨を書く）

直した後に測るもの（各 10〜20 回。回数を書く）:

- (a) すぐ離す押下（`keyboard.press`）で毎回弾が出る
- (b) 押しっぱなし 0.5 秒で 2 発のまま
- (c) 1 発目が出たのを確かめた直後に押すと、約 0.35 秒後に 2 発目が出る（出た時刻も書く）
- (d) 1 発目の後、撃てない間に 3 回押しても、出るのは 1 発だけ
- (e) P で一時停止し、Space を押してから P で再開しても撃たない
- (f) タイトルで Space を押してから Enter で始めても、始めた直後に撃たない
- (g) blur の後、Enter で再開しても撃たない
- (h) スマホ 390x844・hasTouch で、FIRE ボタンを `tap` したとき毎回弾が出る

(e)〜(g) は「押してから次の操作まで 0.2 秒以上空ける」条件で測る（1 フレーム未満で続けて
押す場合は対象外と管理者が決めた）。

`npm test` と `npm run build` の終了コードも書く。報告は `verifier-report.md` の末尾に
「2 回目」の節として足す。返事は 5 行以内。preview サーバは終わったら止める。
ポート 4321 は利用者の dev サーバなので止めない。
